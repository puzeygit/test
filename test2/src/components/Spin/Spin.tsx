import React from 'react'
import { Container, Spinner } from '../UI.styled'

const Spin: React.FC = () => {
  return (
      <Container>
        <Spinner />
      </Container>
  )
}

export default Spin;

